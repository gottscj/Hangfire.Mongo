<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003"
         ToolsVersion="4.0" DefaultTargets="Build">

         <Target Name="Build">

            <Message Text="APPVEYOR: $(APPVEYOR)"/>
            <Message Text="CI: $(CI)"/>
            <Message Text="APPVEYOR_REPO_TAG: $(APPVEYOR_REPO_TAG)"/>
            <Message Text="APPVEYOR_REPO_BRANCH: $(APPVEYOR_REPO_BRANCH)"/>
            <Message Text="APPVEYOR_REPO_COMMIT: $(APPVEYOR_REPO_COMMIT)"/>

         </Target>

</Project>